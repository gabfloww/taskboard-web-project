{
  "name": "taskboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "npm run setup:backend && npm run setup:frontend",
    "setup:backend": "cd backend && npm install && npx prisma generate && npx prisma migrate dev --name init && node src/seed.js",
    "setup:frontend": "cd frontend && npm install",
    "build": "npm run setup:frontend && cd frontend && npm run build && cd ../backend && npx prisma generate",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "dependencies": {
    "express": "^4.21.2",
    "cors": "^2.8.5"
  }
}
